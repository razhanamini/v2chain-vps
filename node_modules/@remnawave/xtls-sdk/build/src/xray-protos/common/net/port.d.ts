import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
export declare const protobufPackage = "xray.common.net";
/** PortRange represents a range of ports. */
export interface PortRange {
    $type: "xray.common.net.PortRange";
    /** The port that this range starts from. */
    From: number;
    /** The port that this range ends with (inclusive). */
    To: number;
}
/** PortList is a list of ports. */
export interface PortList {
    $type: "xray.common.net.PortList";
    range: PortRange[];
}
export declare const PortRange: MessageFns<PortRange, "xray.common.net.PortRange">;
export declare const PortList: MessageFns<PortList, "xray.common.net.PortList">;
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
export type DeepPartial<T> = T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {} ? {
    [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]>;
} : Partial<T>;
export interface MessageFns<T, V extends string> {
    readonly $type: V;
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create(base?: DeepPartial<T>): T;
    fromPartial(object: DeepPartial<T>): T;
}
export {};
//# sourceMappingURL=port.d.ts.map