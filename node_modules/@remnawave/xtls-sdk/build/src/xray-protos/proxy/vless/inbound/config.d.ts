import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { User } from "../../../common/protocol/user";
export declare const protobufPackage = "xray.proxy.vless.inbound";
export interface Fallback {
    $type: "xray.proxy.vless.inbound.Fallback";
    name: string;
    alpn: string;
    path: string;
    type: string;
    dest: string;
    xver: number;
}
export interface Config {
    $type: "xray.proxy.vless.inbound.Config";
    clients: User[];
    fallbacks: Fallback[];
    decryption: string;
    xorMode: number;
    secondsFrom: number;
    secondsTo: number;
    padding: string;
}
export declare const Fallback: MessageFns<Fallback, "xray.proxy.vless.inbound.Fallback">;
export declare const Config: MessageFns<Config, "xray.proxy.vless.inbound.Config">;
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
export type DeepPartial<T> = T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {} ? {
    [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]>;
} : Partial<T>;
export interface MessageFns<T, V extends string> {
    readonly $type: V;
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create(base?: DeepPartial<T>): T;
    fromPartial(object: DeepPartial<T>): T;
}
export {};
//# sourceMappingURL=config.d.ts.map